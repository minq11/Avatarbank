# 나중에 할 일

의도적으로 미뤄둔 것들. 출시 전에 다시 볼 것.

## 1. 사업자 정보 (법적 필수) 🔴

사업자 등록 후 아래를 채워야 함. 현재는 전부 생략된 상태.

- **개인정보처리방침** (`frontend/src/views/PrivacyPage.vue`) — 10항에 TODO 주석으로 자리 표시해 둠.
  - 상호 / 대표자명 / 사업자등록번호 / 통신판매업신고번호 / 사업장 주소
  - 개인정보 보호책임자 성명·직위·연락처 (법적으로 이름과 연락처 기재 의무)
- **푸터** (`frontend/src/App.vue`) — 하단 바에 사업자 정보 한 줄 추가 검토.
- 전자상거래법상 표시 의무 사항 전반 재검토 (유료 결제 도입 시 특히 중요).

## 2. 결제 연동 🔴

- 현재 `SUBSCRIPTION_STUB_PAID_PLANS=True` → **유료 플랜을 과금 없이 즉시 활성화**하는 데모 상태.
  운영 전환 시 `False` 로 바꾸고 결제 웹훅에서만 구독을 활성화해야 함.
- `.env` 에 PayPal 키가 이미 있으나 미연동. Stripe 등 대안 검토.
- 결제 도입 시 **환불 규정**을 이용약관(`TermsPage.vue` 제4조)에 추가해야 함.
  현재는 "결제 미연동 상태이며 소급 과금하지 않는다"고만 명시돼 있음.

## 3. 문의 메일 — Gmail 앱 비밀번호 🟡

문의 기능은 완성됐지만 **메일 발송은 아직 동작하지 않음** (`.env` 의 `SMTP_PASSWORD` 가 비어 있음).

1. `gooddonutsyh@gmail.com` 계정에 2단계 인증 활성화
2. Google 계정 → 보안 → 앱 비밀번호 발급
3. `.env` 의 `SMTP_PASSWORD=` 에 발급받은 값 입력

그 전까지의 동작: 문의는 DB(`inquiries` 테이블)에 정상 저장되고 `/admin/inquiries` 에서 볼 수 있음.
접수 알림 메일만 안 나가며, 관리자가 답장을 시도하면 503 + "SMTP 설정을 확인해 주세요" 표시.

- 문의량이 늘면 `smtplib` 동기 호출을 `BackgroundTasks` 나 큐로 옮길 것 (`backend/app/mailer.py` 주석 참고).
- 장기적으로는 Gmail SMTP 대신 전용 발송 서비스(SES, Resend 등) 고려 — 발송량 제한·스팸 분류 이슈.

## 4. 소스 이미지를 S3 URL 방식으로 전환 🟡

현재 `backend/app/fal_client.py`의 `resolve_source_image()`는 이미지를 **base64 data URI**로
만들어 fal 요청 본문에 통째로 실어 보낸다. 로컬 개발에서도 fal이 접근 가능하게 하려고 택한 방식.

문제:
- 기본 이미지(`assets/itoi_example.png`, 691KB)가 **매 요청마다 ~920KB**의 본문으로 나간다.
  (인코딩 결과는 프로세스 메모리에 캐시되지만 업로드 대역폭은 매번 소모)
- 사용자가 올린 S3 이미지는 **S3에서 내려받아 → base64 인코딩 → 다시 업로드**하는 왕복이 생긴다.
  S3 presigned URL은 이미 공개 접근이 가능하므로 이 왕복은 불필요하다.

전환 방향:
- 운영 환경에서는 `image_url`에 **presigned S3 URL을 그대로 전달** (fal이 직접 받아감).
- 기본 이미지도 S3에 올려두고 presigned URL 사용.
- 로컬 개발처럼 외부에서 접근 불가한 환경만 data URI로 폴백하도록 설정 플래그
  (예: `FAL_IMAGE_TRANSPORT=url|data_uri`) 추가.
- `resolve_source_image()` 한 곳만 고치면 되도록 이미 분리해 둠.

## 5. DB 마이그레이션 🟡

- `inquiries` 테이블은 현재 `Base.metadata.create_all` 로만 생성됨 (`backend/app/main.py` startup).
  기존 운영 DB 가 있다면 Alembic 마이그레이션을 도입해 관리할 것.
- 마찬가지로 `plans` / `subscriptions` / `generation_templates` / `redeem_codes` 도 동일 상태.

## 6. 모더레이션 강화 🟡

`backend/app/moderation.py` 의 차단어 목록은 **전부 제거된 상태**다. `check_prompt()` 는 현재
모든 프롬프트를 통과시키는 no-op 훅이다.

- 필터 정책은 직접 정의할 것. `check_prompt()` 안에만 로직을 넣으면
  크리에이터 생성·팬 리딤 생성 전 경로(main.py 1곳 + studio.py 4곳)에 한 번에 적용된다.
- fal 쪽 결과 이미지 검사(`enable_safety_checker`)는 코드 필터와 별개로 여전히 강제 ON 이다.
  이것도 끄려면 `fal_client.run_generation_sync` 의 인자를 손볼 것.

## 7. 레이트리밋 Redis 전환 🟡

`backend/app/rate_limit.py` 의 `SlidingWindowLimiter` 는 **프로세스 로컬 메모리** 기반.
uvicorn 워커가 여러 개면 인스턴스별로 따로 계산되어 실제 제한이 워커 수만큼 느슨해짐.

- 적용 대상: 리딤 조회/생성(`/r/*`), 문의 접수(`POST /inquiries`)
- `.env` 에 `REDIS_URL` 은 이미 있음 → Redis 기반으로 교체.

## 8. 관리자 권한 방식 🟢

- 현재 관리자 판별은 `ADMIN_EMAIL_WHITELIST` 환경변수 기반
  (`backend/app/dependencies.py` 의 `is_admin_email` / `require_admin`).
- 프론트(`frontend/src/stores/auth.ts`)에는 `minku0128@naver.com` 이 **하드코딩**되어 있어
  백엔드 화이트리스트와 이중 관리 중. `users.role` 에 admin 을 쓰는 방식으로 통일 검토.
- 프론트 판별은 메뉴 노출용일 뿐이고 실제 차단은 백엔드에서 하므로 보안 문제는 아님.

## 9. 기타 🟢

- `App.vue` 언어 스위처(EN/KO/JA)는 UI만 있고 실제 i18n 미적용. 현재 전 페이지 한국어 고정.
- 정책 문서들의 "시행일 2026년 8월 5일"은 실제 서비스 오픈일에 맞춰 갱신할 것.
- 인스타그램 링크(`@avatarbank_official`)가 실제 운영 계정인지 확인 — 푸터 2곳에서 사용 중.
